# CodeAlpha_Basic-Chatbot
This project is a simple rule-based chatbot developed in Python. It responds to predefined user inputs such as "hello," "how are you," and "bye" using if-else statements. The program uses a function, a while loop, and input/output operations to simulate a basic conversation until the user chooses to exit.
